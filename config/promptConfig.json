{
  "core_command": {
    "system": [
      "Transform Information according to Task.",
      "Use Tool output if present.",
      "Keep answers concise and complete.",
      "Use at most two short sentences unless the task asks for more.",
      "Output only the result."
    ],
    "user_template": [
      "Do:",
      "{{command}}",
      "",
      "Text:",
      "{{information}}"
    ],
    "user_template_with_skill_context": [
      "Do:",
      "{{command}}",
      "",
      "Text:",
      "{{information}}",
      "",
      "Context:",
      "{{skill_context}}"
    ]
  }
}
